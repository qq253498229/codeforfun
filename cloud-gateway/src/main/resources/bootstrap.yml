eureka:
  client:
    service-url:
      defaultZone: http://cloud-eureka:8761/eureka/
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: cloud-gateway
      profile: dev
  application:
    name: cloud-gateway
server:
  port: 8080